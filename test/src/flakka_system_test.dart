// ignore_for_file: prefer_const_constructors
import 'package:flakka_system/flakka_system.dart';
import 'package:test/test.dart';

void main() {
  group('FlakkaSystem', () {
    test('can be instantiated', () {
      expect(FlakkaSystem(), isNotNull);
    });
  });
}
